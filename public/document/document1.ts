export const documentCode = `#  \`my_sum\` 関数を用いた配列の合計計算プログラム
## 1. 概要
本プログラムは、整数型の配列を受け取り、その要素の合計を計算する \`my_sum\` 関数を実装する。  
加えて、配列の内容を表示し、計算された合計値を出力する。
## 2. 動作仕様
1. \`ary[5] = {1, 2, 3, 4, 5}\` を初期化し、\`my_sum\` 関数を用いてその合計値を計算する。  
2. \`my_sum\` 関数は、配列の先頭アドレスと要素数を引数として受け取り、合計値を返す。  
3. \`main\` 関数では、配列の各要素を表示し、計算結果の合計値を出力する。
## 3. 入出力仕様
### 入力:
- 本プログラムはユーザーからの入力を必要とせず、固定の配列 \`{1, 2, 3, 4, 5}\` を処理する。
### 出力:
- 配列の各要素を表示する（例: \`1 2 3 4 5\`）
- 配列の合計値を表示する（例: \`sum = 15\`）
## 4. 関数仕様
### 関数名: \`my_sum\`
- **引数:**
  - \`int* ary\` : 配列の先頭アドレス
  - \`int size\` : 配列の要素数
- **処理:**
  - 配列の要素を順に加算し、合計値を計算する
- **返り値:**
  - 計算された合計値 (\`int\` 型)
`;
